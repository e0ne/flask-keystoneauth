============
Installation
============

At the command line::

    $ pip install flask-keystoneauth

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv flask-keystoneauth
    $ pip install flask-keystoneauth
